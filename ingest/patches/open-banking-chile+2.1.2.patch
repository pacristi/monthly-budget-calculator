diff --git a/node_modules/open-banking-chile/dist/index.js b/node_modules/open-banking-chile/dist/index.js
index 74a84cf..439b05e 100644
--- a/node_modules/open-banking-chile/dist/index.js
+++ b/node_modules/open-banking-chile/dist/index.js
@@ -346,7 +346,7 @@ async function apiPost(page, path2, body = {}) {
 }
 async function bchileLogin(page, rut, password, debugLog, doSave) {
   debugLog.push("1. Navigating to bank homepage...");
-  await page.goto(BANK_URL, { waitUntil: "networkidle2", timeout: 45e3 });
+  await page.goto(BANK_URL, { waitUntil: "domcontentloaded", timeout: 12e4 });
   await delay(3e3);
   await doSave(page, "01-homepage");
   try {
@@ -537,7 +537,7 @@ async function fetchAccountMovements(page, products, fullName, rut, debugLog) {
   });
   if (unique.length === 0) return { movements: [] };
   const baseUrl = page.url().split("#")[0];
-  await page.goto(`${baseUrl}#/movimientos/cuenta/saldos-movimientos`, { waitUntil: "networkidle2", timeout: 3e4 });
+  await page.goto(`${baseUrl}#/movimientos/cuenta/saldos-movimientos`, { waitUntil: "domcontentloaded", timeout: 9e4 });
   await delay(5e3);
   const movements = [];
   let balance;
